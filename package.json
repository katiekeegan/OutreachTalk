{
  "name": "outreach-talk-cloudflare",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "Cloudflare Worker + D1 deployment for the moderated MoMath OutreachTalk exercise.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node --test",
    "check": "node --check src/worker.js && node --check src/db.js && node --check src/auth.js && node --check public/audience.js && node --check public/moderator/moderator.js && node --check public/play/app.js && node --check public/play/live.js && node --check public/lab/play.js && node --check public/vendor/qr-svg.js",
    "db:migrate:local": "wrangler d1 migrations apply DB --local",
    "db:migrate:remote": "wrangler d1 migrations apply DB --remote"
  },
  "devDependencies": {
    "wrangler": "^4.102.0"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
